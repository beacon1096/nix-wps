name: Build wpsoffice-xa

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - system: x86_64-linux
            os: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v12
        with:
          extra-conf: |
            accept-flake-config = true
            allow-import-from-derivation = true

      - name: Configure Nix for unfree packages
        run: |
          mkdir -p ~/.config/nixpkgs
          echo '{ allowUnfree = true; }' > ~/.config/nixpkgs/config.nix

      - name: Build wpsoffice-xa
        run: nix build .#packages.${{ matrix.system }}.wpsoffice-xa --print-out-paths --impure
